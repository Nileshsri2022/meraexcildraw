import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nilesh.meraexcalidraw',
  appName: 'Mera Excalidraw',
  webDir: 'dist'
};

export default config;
